func ConnectDB() {
    dsn := "
    host=localhost 
    user=postgres 
    password=postgres123
    dbname=roamify
    port=5432 sslmode=disable"
    db, err := gorm.Open(postgres.Open(dsn), &gorm.Config{})
}